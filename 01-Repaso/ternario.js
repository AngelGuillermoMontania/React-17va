// 5 === "5" ? "Es 5" : "No es 5";

// undefined ? "Es true" : "Es false";

// undefined == null ? "Es true" : "Es false";

// undefined === null ? "Es true" : "Es false";

// "" == false ? "Es true" : "Es false";

// const name = "";

// name ? `Hola ${name}` : ""

// const arr = [0];
// arr[0] ? arr[0] : "";
