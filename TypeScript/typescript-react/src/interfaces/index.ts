export interface task {
  title: string;
  description: string;
  id: number;
  done: boolean;
}

export interface taskList {}

export interface taskListState {}

// function (n: number): void {    Function que no retorna, es del tipo VOID
//   const num = n + 5;
// }
